/* What typescript does : Static checking of datatypes
-> Ts code is transpiled into js code
->Ts is just a dev tool, code still runs in js 
*/
let user = {
    name:"Ali",
    age:10
}

console.log(user.name)

